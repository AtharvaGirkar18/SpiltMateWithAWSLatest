# ===================================
# PRODUCTION ENVIRONMENT VARIABLES
# Copy this to .env on EC2 instance
# ===================================

# MongoDB Atlas (same as dev)
MONGODB_URI=mongodb+srv://node:node@cluster0.yilta.mongodb.net/splitmate

# PostgreSQL RDS (Production)
DB_HOST=splitmate-db.c9yrubuuio83.us-east-1.rds.amazonaws.com
DB_PORT=5432
DB_NAME=splitmate
DB_USER=postgres
DB_PASSWORD=SplitMate2025!Secure

# AWS Configuration
AWS_REGION=us-east-1
AWS_ACCESS_KEY_ID=YOUR_ACCESS_KEY_HERE
AWS_SECRET_ACCESS_KEY=YOUR_SECRET_KEY_HERE
AWS_S3_BUCKET=splitmate-profile-images

# AWS Cognito (UPDATE THESE WITH YOUR EC2 IP/DOMAIN)
COGNITO_DOMAIN=https://us-east-1dnpdmzhmx.auth.us-east-1.amazoncognito.com
COGNITO_CLIENT_ID=27pim7sjr8hgard9l21phj9pt4
COGNITO_CLIENT_SECRET=YOUR_CLIENT_SECRET_HERE

# ⚠️ IMPORTANT: Update these with your EC2 public IP or domain
# Option 1: Using EC2 Public IP (temporary)
COGNITO_REDIRECT_URI=http://YOUR-EC2-PUBLIC-IP:3000/auth/callback
COGNITO_LOGOUT_URI=http://YOUR-EC2-PUBLIC-IP:3000

# Option 2: Using Custom Domain (recommended)
# COGNITO_REDIRECT_URI=https://splitmate.com/auth/callback
# COGNITO_LOGOUT_URI=https://splitmate.com

# Session Secret (generate a strong random key)
SESSION_SECRET=CHANGE_THIS_TO_A_RANDOM_SECRET_KEY_IN_PRODUCTION

# Lambda Debug (disable in production)
LAMBDA_DEBUG=0

# Node Environment
NODE_ENV=production

# Port (can be 3000, or 80 if running as root)
PORT=3000
